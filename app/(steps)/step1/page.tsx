import BtnContinue from '@/app/components/buttons/btnContinue';
import { TextField } from '@/app/components/textField';

export default function Step1({ params }: { params: { referer: string } }) {
  console.log(params.referer, 'params.referer');

  return <div>23</div>;
}
